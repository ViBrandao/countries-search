import { SignInButton } from '../SignInButton';

export function Header() {
    return (
        <header>
            <div >
                <SignInButton />
            </div>
        </header>
    );
}